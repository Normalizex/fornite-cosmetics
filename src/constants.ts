import { createTheme } from '@mui/material/styles';

export const endpoint = 'https://fortnite-api.com/v2';

export const theme = createTheme({
    palette: {
        mode: 'dark'
    }
});

export const MAXIMUM_PAGE_ITEMS = 48;
